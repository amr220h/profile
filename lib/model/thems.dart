// ignore_for_file: constant_identifier_names

import 'package:flutter/material.dart';

const backgroundColor = Colors.black;
const Color TExt = Colors.white; // Use 0xFF for full opacity followed by the hex code
const WIdget = Color.fromRGBO(30, 58, 95,0.5);
const three = Color.fromRGBO(	4, 30, 66, 1);
